---
title: Inn & maids
description: Where death respawns you, how sitting works, how the maids Miriel and Cocoly's service triggers, and how to order food and drinks.
---

import { itemsById, formatCoins } from '../../../../lib/gamedata.js';
import { itemName } from '../../../../lib/en.js';

The inn is where death respawns you, and today the only place you can **sit
down**. Two maids, Miriel (protocol v46) and Cocoly (protocol v48), work the
inn's ground floor and greet players who just respawned or just sat down.

## Respawning

Dying no longer sends you back to the town spawn point — you land in the
inn's sick room instead (protocol v46):

- The sick room has **4 beds**. The server picks one that's free and poses
  the character lying in it.
- If all 4 are taken, the character stands beside the beds instead of
  clipping into anyone.
- Bed positions follow the furniture placed in the map editor, so moving the
  furniture moves the respawn point.
- Starting protocol v47, a maid recognizes a player who just respawned in a
  bed and walks over to greet them; the bed respawn itself already worked
  before that, the maid just wasn't showing up for it yet.

## Sitting

Any chair marked with the "sit" interaction in the furniture catalog can be
clicked to sit (protocol v46):

- Clicking a chair plays a sit-down animation; chatting while seated switches
  to a seated chat pose.
- Standing up plays a stand-up clip first, then the character steps to the
  free cell beside the chair — the cell directly in front of a chair is
  usually a table, so it steps aside instead of into it.
- Clicking the same chair again mid-walk no longer instantly pops the
  character back up (fixed in protocol v48).

## Maid service

Sitting on a chair near a maid queues a visit (protocol v48): she walks over
(some chairs have a curated stand spot in the data, others get a computed
one) and greets you for your order.

Miriel and Cocoly share one **visit claim board** — whether it's a bedside
respawn greeting or a seated guest, only the maid who claims a given bed or
player first will answer it, so you never get double-greeted or skipped
entirely.

Like other shop NPCs, the maids never open a trade window on their own — you
have to click them first before the trade UI appears (protocol v48).

## Ordering food and drinks (protocol v51)

Order from a maid while seated and she makes her usual kitchen round trip,
then returns to set the plate on the table. Several chairs at one table each
get their own spot along the edge instead of stacking. Click the plate to
eat — satiation fills to the cap, and HP recovers based on the actual gain.
The emptied plate stays on the table and can't be eaten again. If you leave
or disappear, the maid clears the plate 8 seconds later, and the server
removes it automatically after 90 seconds regardless; a meal is cleared
after 30 minutes even if you're still seated. NPC guests eat automatically 6
seconds after being served.

Every dish is a serve-only item, not sold in any shop:

| Dish | Details |
|---|---|
| {itemName('chicken_rice')} | {formatCoins(itemsById.get('chicken_rice').basePrice)}, {itemsById.get('chicken_rice').nutrition} nutrition |
| {itemName('chicken_curry')} | {formatCoins(itemsById.get('chicken_curry').basePrice)}, {itemsById.get('chicken_curry').nutrition} nutrition |
| {itemName('fried_rice')} | {formatCoins(itemsById.get('fried_rice').basePrice)}, {itemsById.get('fried_rice').nutrition} nutrition |
| {itemName('vegetable_rice')} | {formatCoins(itemsById.get('vegetable_rice').basePrice)}, {itemsById.get('vegetable_rice').nutrition} nutrition |

Every chair also has a drink slot beside its dish slot (to the guest's
right), so ordering food and a drink together doesn't overlap — one kitchen
trip brings both back. A drink only adds its own nutrition, and the cup
stays behind once it's finished.

- {itemName('beer')} ({formatCoins(itemsById.get('beer').basePrice)}) — 1 alcohol unit.
- {itemName('wine')} ({formatCoins(itemsById.get('wine').basePrice)}) — 2 alcohol units.

### Getting tipsy

Alcohol units drunk within ten minutes set your stage; the three stages are
exclusive, so only the latest one sticks.

| Units | Stage | Effect |
|---|---|---|
| 1 | Tipsy | Move/attack speed ×1.1 |
| 2 | Drunk | Move/attack/carry ×0.85, hit roll −2 |
| 3+ | Wasted | Move/attack/carry ×0.6, hit roll −5, click-to-move target drifts 1–2.5m off target |
