---
title: Dungeons
description: Dungeon locations, floor counts, bosses and the chest drop rules.
---

import FilterTable from '../../../../components/FilterTable.astro';
import { dungeons, monsters, items, lockedFloors } from '../../../../lib/gamedata.js';
import { itemName } from '../../../../lib/en.js';

export const signature = (d) => (d.chestDrops.length ? d.chestDrops.map(itemName).join(', ') : '—');

**{dungeons.length} dungeons** are open. Dungeons are **seed-determined**: the
same entrance always has the same layout and is never regenerated.

## The dungeons

<FilterTable
  placeholder="Search dungeons…"
  columns={[
    { key: 'name', label: 'Name' },
    { key: 'tier', label: 'Tier', align: 'center' },
    { key: 'floors', label: 'Floors', align: 'right' },
    { key: 'boss', label: 'Boss' },
    { key: 'signature', label: 'Guaranteed drop' },
    { key: 'coords', label: 'Coordinates' },
  ]}
  rows={dungeons.map((d) => ({
    name: d.name,
    tier: `T${d.chestTier}`,
    floors: `${d.floors}`,
    boss: d.boss ? monsters.find((m) => m.id === d.boss)?.name ?? d.boss : '—',
    signature: signature(d),
    coords: `${d.x}, ${d.z}`,
  }))}
/>

The world map records dungeon entrances **you have already found** — you have
to walk to one the first time.

## Bosses

<FilterTable
  placeholder="Search bosses…"
  columns={[
    { key: 'name', label: 'Boss' },
    { key: 'dungeon', label: 'Dungeon' },
    { key: 'level', label: 'Level', align: 'right' },
    { key: 'hp', label: 'Health', align: 'right' },
    { key: 'guard', label: 'Guard', align: 'right' },
    { key: 'atk', label: 'To-hit', align: 'right' },
    { key: 'dmg', label: 'Damage', align: 'center' },
  ]}
  rows={dungeons.filter((d) => d.boss).map((d) => {
    const m = monsters.find((x) => x.id === d.boss);
    return {
      name: m.name, dungeon: d.name,
      level: m.level, hp: m.healthEffective, guard: m.guard,
      atk: `+${m.attackBonusEffective}`, dmg: m.damageRollEffective,
    };
  })}
/>

The **Orc Warlord** is a step up: its 120 health is set directly in the data
(not the 45 the level would derive), with Guard 13, +6 to hit and 2d8 damage.
Orc Warrens runs 10 floors, so put a full leather set together before going
down.

A slain boss doesn't respawn on a timer — its floor holds it dead until the
next **sunset dungeon reset** (once per game day), which also teleports
everyone still in that dungeon back to its entrance with a distant roar.
Opening the chest no longer requires killing the boss or standing next to it
— see "Keys and locked floors" below. The weapon each boss used to drop
itself (Goblin Sword, Iron Sword, Steel Longsword) is now a guaranteed chest
drop instead; see the table below.

## Keys and locked floors

Every **5th floor**'s stair-room exit has a locked door that always exists,
blocking the way on completely:

<FilterTable
  placeholder="Search dungeons…"
  columns={[
    { key: 'name', label: 'Dungeon' },
    { key: 'floors', label: 'Locked floors' },
    { key: 'chest', label: 'Key for the chest' },
  ]}
  rows={dungeons
    .filter((d) => d.keyPrefix)
    .map((d) => {
      const locked = lockedFloors(d);
      return {
        name: d.name,
        floors: locked.map((f) => `Floor ${f}`).join(', '),
        chest: itemName(`${d.keyPrefix}_${locked[locked.length - 1]}`),
      };
    })}
/>

- **Keys drop in the section just before their locked door** (for example,
  Orc Warrens floors 1–4 drop the floor-5 key, floors 6–9 drop the floor-10
  key). Monsters killed in that section have a **5%** chance to drop the
  matching key; breaking a barrel, crate or clutter chest there is **1%**. The
  roll is skipped once a character already holds that key, or has already
  opened that dungeon's chest tonight.
- Keys drop on the ground for anyone to pick up; **a floor-5 key won't open a
  floor-10 door.**
- Holding the key and clicking the locked door opens it. **The door doesn't
  consume the key** — dying or leaving and coming back still works, and keys
  never expire. An opened door **stays open for 30 seconds** before
  auto-closing, so someone without a key can slip in behind you during that
  window, but once it closes they're stuck on whichever side they ended up
  on.
- **The key for the dungeon's deepest locked door** (Old Crypt floor 5, Orc
  Warrens floor 10, Ogre Stronghold floor 15) is the ticket to the chest —
  carry it to the bottom floor and open the chest without ever killing the
  boss. Opening it spends one of **every key type you're carrying** for that
  dungeon. The boss still guards the chest room, so opening it safely
  usually still means dealing with it.

## Chest drop rules

Each dungeon's chest drops come in three parts:

1. **Signature drop(s)** — one or two guaranteed items per dungeon, the
   "Guaranteed drop" column above; the three boss weapons now count among
   them.
2. **Independent rolls** — every other piece in that tier's pool rolls
   separately at its own chance.
3. **Lower-tier carry-over** — equipment from tiers below the dungeon each get
   an extra 10% roll, so you can fill gaps in a higher dungeon.

Gold is `depth × 500` to `depth × 1500`.

The target is roughly **five runs on average** to collect everything a tier
introduces.

<FilterTable
  placeholder="Search equipment…"
  columns={[
    { key: 'name', label: 'Equipment' },
    { key: 'tier', label: 'Chest tier', align: 'center' },
    { key: 'chance', label: 'Drop chance', align: 'right' },
    { key: 'guard', label: 'Guard', align: 'right' },
    { key: 'cha', label: 'CHA', align: 'right' },
    { key: 'effect', label: 'Effect' },
  ]}
  rows={items
    .filter((i) => i.chestTier)
    .sort((a, b) => a.chestTier - b.chestTier || (b.guard ?? 0) - (a.guard ?? 0))
    .map((i) => ({
      name: i.name,
      tier: `T${i.chestTier}`,
      chance: i.chestChance ? `${Math.round(i.chestChance * 100)}%` : 'Guaranteed',
      guard: i.guard ?? '—',
      cha: i.chaBonus ? `+${i.chaBonus}` : '—',
      effect: i.hasSustenance ? 'Satiation drain ×0.75' : '—',
    }))}
/>

:::note[The only weapons in chests are the three boss weapons]
Weapons are normally obtained from "the monster carrying it drops it" — the
exception is the Goblin Sword, Iron Sword and Steel Longsword, which are now
guaranteed chest drops instead of boss drops. Every other weapon still works
the old way: what you see a monster carrying is what it can drop, and chests
don't carry it.
:::

## What you'll meet down there

<FilterTable
  placeholder="Search monsters…"
  columns={[
    { key: 'name', label: 'Monster' },
    { key: 'level', label: 'Level', align: 'right' },
    { key: 'depth', label: 'Spawn depth' },
    { key: 'weight', label: 'Weight', align: 'right' },
    { key: 'hp', label: 'Health', align: 'right' },
    { key: 'dmg', label: 'Damage', align: 'center' },
  ]}
  rows={monsters
    .filter((m) => m.dungeonMinDepth !== null)
    .sort((a, b) => a.dungeonMinDepth - b.dungeonMinDepth || a.level - b.level)
    .map((m) => ({
      name: m.name, level: m.level,
      depth: `Floors ${m.dungeonMinDepth} – ${m.dungeonMaxDepth}`,
      weight: m.dungeonWeight, hp: m.healthEffective, dmg: m.damageRollEffective,
    }))}
/>

## Other things down there

- **Barrels and crates break** — click one and your character walks up and
  swings once. Treasure chests don't break.
- **Chests are opened** — click one and your character walks over and lifts the
  lid. A real coin pile drops in front of it; picking it up credits 1–10 copper
  straight to your wallet without taking a bag slot.
- **Corridors have doors** — where a corridor opens into a room there's a
  clickable wooden double door.
- **Corridor and room walls differ** — rooms use medieval stone, corridors use
  rock. Useful for telling where you are.
